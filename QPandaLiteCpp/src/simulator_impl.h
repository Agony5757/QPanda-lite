#pragma once

// Backward-compatibility wrapper — includes the split headers.
// New code should include the specific header it needs.
#include "simulator_common.h"
#include "simulator_statevector_impl.h"
#include "simulator_density_op_impl.h"
