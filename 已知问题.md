# 已知问题（CI 修复轮次，2026-04-14）

## 分支与 PR 关联

- 当前 PR 使用分支：`fix/ci-r1r3r10`（PR #139）。
- 本文只记录与 bug 相关的已知问题与后续建议，不记录协作文档流程细节。

## 1) worktree 合并状态与简测结果（最新）

- `cli/worktree-2026-04-14T14-50-31`：无未提交代码改动（仅本地协作文档 `来自A.md`）。
- `cli/worktree-2026-04-14T14-51-58`：已合并 4 个测量相关文件改动：
  - `qpandalite/algorithmics/measurement/classical_shadow.py`
  - `qpandalite/algorithmics/measurement/state_tomography.py`
  - `qpandalite/test/test_measurement_basis_rotation.py`
  - `qpandalite/test/test_measurement_shadow.py`

简测结果（合并后）：

- 命令：
  - `uv run --with pytest --with pytest-cov pytest qpandalite/test/test_measurement_shadow.py qpandalite/test/test_measurement_basis_rotation.py qpandalite/test/test_measurement_tomography.py -q`
- 结果：`43 passed, 2 warnings`

备注：此前稳定复现的测量层 7 项失败（shadow 5 + basis rotation 1 + tomography 1）属于上述改动域；在本轮简测中已不复现。全量 CI 仍以远端回报为准。

## 2) Grover（R9）位序相关潜伏问题

存在一组互相掩盖的位序问题（仅在非回文目标态更容易暴露）：

1. `qpandalite/algorithmics/circuits/grover_oracle.py` 中 `marked_state` 位提取存在 MSB/LSB 约定风险。
2. `qpandalite/test/test_circuits_grover_oracle.py` 中数据态聚合曾出现“去 ancilla 位”的位索引不一致风险。

说明：这组问题在特定用例（如回文 bit pattern）下可能“看起来通过”，但属于真实技术债，建议在 follow-up PR 里用 non-palindrome regression test 锁定。

## 3) CONTROL 序列化/执行路径异常（待确认）

排查记录中出现过 `CONTROL ... ENDCONTROL` 未按预期进入 QASM/执行路径的现象，伴随 Grover 放大退化为近均匀分布。  
该项目前属于“已观察到但需进一步复现确认”的问题，建议单独最小复现并补回归测试后再修复。

## 建议的后续顺序

1. 先观察本分支最新 CI 回报，确认测量层失败是否随本次合并一起消失。
2. 以独立 follow-up PR 处理 Grover 位序潜伏问题（含非回文回归用例）。
3. 对 CONTROL 序列化问题做最小复现与定点修复，避免与前两项交叉干扰。
