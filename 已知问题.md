# 已知问题（CI 修复轮次，2026-04-14）

## 分支与 PR 关联

- 当前 PR 使用分支：`fix/ci-r1r3r10`（PR #139）。
- 本文只记录与 bug 相关的已知问题与后续建议，不记录协作文档流程细节。

## 1) 测量层残留失败（7 项）

当前 `fix/ci-r1r3r10` 连续多次 push 后，CI 仍稳定为：

- `7 failed / 927 passed / 1 deselected / 3 xfailed`

残留失败全部在测量层：

- classical shadow（5）
  - `test_measurement_shadow.py::test_z_on_zero`
  - `test_measurement_shadow.py::test_x_on_plus`
  - `test_measurement_shadow.py::test_z_on_plus_is_zero`
  - `test_measurement_shadow.py::test_y_on_iplus`
  - `test_measurement_shadow.py::test_zzz_on_computational`
- basis rotation（1）
  - `test_measurement_basis_rotation.py::test_list_basis`
- state tomography（1）
  - `test_measurement_tomography.py::test_ghz3_purity`

已知归因：这些问题属于测量模块修复范围（对应 #138 的改动域），不在 #139 既有提交覆盖面内，因此在该分支上会持续复现。

## 2) Grover（R9）位序相关潜伏问题

存在一组互相掩盖的位序问题（仅在非回文目标态更容易暴露）：

1. `qpandalite/algorithmics/circuits/grover_oracle.py` 中 `marked_state` 位提取存在 MSB/LSB 约定风险。
2. `qpandalite/test/test_circuits_grover_oracle.py` 中数据态聚合曾出现“去 ancilla 位”的位索引不一致风险。

说明：这组问题在特定用例（如回文 bit pattern）下可能“看起来通过”，但属于真实技术债，建议在 follow-up PR 里用 non-palindrome regression test 锁定。

## 3) CONTROL 序列化/执行路径异常（待确认）

排查记录中出现过 `CONTROL ... ENDCONTROL` 未按预期进入 QASM/执行路径的现象，伴随 Grover 放大退化为近均匀分布。  
该项目前属于“已观察到但需进一步复现确认”的问题，建议单独最小复现并补回归测试后再修复。

## 建议的后续顺序

1. 先合并或等效引入测量层修复（#138 改动域），清除 7 项稳定失败。
2. 再以独立 follow-up PR 处理 Grover 位序潜伏问题（含非回文回归用例）。
3. 最后对 CONTROL 序列化问题做最小复现与定点修复，避免与前两项交叉干扰。
